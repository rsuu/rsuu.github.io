---
layout: page
date: 20220501000000
title: paste
categories:
tags: [cmd]
udc:
---

## paste

```sh
paste a b
    # 左右拼接文本

paste a b > c
    # 把 a 和 b 中的内容按行合并后输出到 c 里

paste a b
    # 把 a 和 b 中的内容按行合并后输出到屏幕

paste -d= a b
    # 把 a 和 b 中的内容按行合并并用 = 符号隔开



```
